# 复制本文件为 signing.local.env（同目录），填你的 Developer ID 证书名。
# 查证书名：security find-identity -v -p codesigning
# 作用：本地 macOS 打包用此证书签名，让辅助功能授权在版本更新后保留。
# signing.local.env 已 gitignore，不会提交。
APPLE_SIGNING_IDENTITY="Developer ID Application: Your Name (TEAMID)"
